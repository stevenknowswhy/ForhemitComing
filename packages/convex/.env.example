# Convex CLI (deploy / codegen). Copy to .env.local — do not commit secrets.
# Same deployment as NEXT_PUBLIC_CONVEX_URL in apps/admin and apps/marketing.
#
# CONVEX_DEPLOYMENT=dev:your-deployment-name
# CONVEX_DEPLOY_KEY=dev:deployment|...   # CI / deploy only; rotate if exposed
